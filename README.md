# Recipe App Backend - Node.js & TypeScript

A robust REST API built with **Express 5**, **Mongoose**, and **Zod** for a recipe management application. This backend handles user authentication, ingredient tracking, and recipe creation with full TypeScript support.

## 🚀 Getting Started

### 1. Installation

Clone the repository and install the dependencies. You can install everything with this single command:

```bash
npm i bcryptjs cors dotenv express jsonwebtoken mongoose zod && npm i -D @types/cors @types/express @types/jsonwebtoken @types/node nodemon ts-node tsx typescript